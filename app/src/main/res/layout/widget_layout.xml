<!-- layout/widget_layout.xml -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!-- Це буде ваша кнопка з трьома крапками -->
    <LinearLayout
        android:id="@+id/three_dots_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:visibility="visible">

        <TextView
            android:id="@+id/three_dots"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="⋯"
            android:textSize="30sp"
            android:layout_marginTop="10dp"
            android:layout_marginBottom="10dp"
            android:textColor="@android:color/black"/>
    </LinearLayout>

    <!-- Вміст віджета (спочатку невидимий) -->
    <LinearLayout
        android:id="@+id/full_widget_content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:visibility="gone"
        android:padding="16dp">

        <!-- Весь вміст, який з'явиться після натискання -->
        <TextView
            android:id="@+id/widget_info"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Ваш вміст тут!"
            android:textSize="18sp"/>

        <!-- Можна додати інші елементи -->
    </LinearLayout>

</LinearLayout>
